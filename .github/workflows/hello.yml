name: hello

on: 
  push:
    branches: [main]


jobs:
  greets:
    runs-on: ubuntu-latest
    steps:
      - name: say hello to the user
        run: echo "Hello Shuvradeep"

      - name: say hello to the username
        run: echo "Hello $USER"

  details:
    runs-on: ubuntu-latest
    steps:
      - name: Want to know the Version of Python and docker
        run: docker --version && python --version
    
